export const FlightsListSelector = (state) => state.flightList;

export const currentDateSelector = (state) => state.currentDate;

export const getValueIsFetching = (state) => state.isFetching;

export const getFilteredData = (state) => state.searchFlight;

export const showCalendarSelector = (state) => state.showCalendar;
